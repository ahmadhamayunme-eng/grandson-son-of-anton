// Anton Chat — entry-point script.
// Real functionality (SSE, composer, switcher, etc.) lands in later phases.
// Phase 1 just confirms the script wired up correctly.
(function () {
  if (window.console && console.log) {
    console.log('[Anton Chat] Phase 1 shell loaded');
  }
})();
